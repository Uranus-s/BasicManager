<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.basic.dao.sysNotice.mapper.SysNoticeTargetMapper">

    <delete id="deleteByNoticeId">
        DELETE FROM sys_notice_target
        WHERE notice_id = #{noticeId}
    </delete>

    <insert id="insertBatch">
        INSERT INTO sys_notice_target (notice_id, target_type, target_id)
        VALUES
        <foreach collection="targets" item="target" separator=",">
            (#{target.noticeId}, #{target.targetType}, #{target.targetId})
        </foreach>
    </insert>

    <select id="selectByNoticeId" resultType="com.basic.dao.sysNotice.entity.SysNoticeTarget">
        SELECT
            notice_id AS noticeId,
            target_type AS targetType,
            target_id AS targetId
        FROM sys_notice_target
        WHERE notice_id = #{noticeId}
        ORDER BY target_type, target_id
    </select>
</mapper>
